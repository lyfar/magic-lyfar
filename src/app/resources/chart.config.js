// Default chart configuration
export const chart = {
  height: 400,
  variant: "gradient", // flat | gradient | outline
  mode: "default", // default mode for data-viz attribute
  tick: {
    fill: "var(--neutral-on-background-weak)",
    fontSize: 12
  },
  axisLine: {
    stroke: "var(--neutral-alpha-medium)"
  },
  tickLine: false
}; 